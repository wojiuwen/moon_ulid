// Blackbox tests run against the package's public API. Use `@moon_ulid` to import.
// Prefer high-level behaviors over implementation details.
// Run `moon test` to execute and `moon test --update` to refresh snapshots.
